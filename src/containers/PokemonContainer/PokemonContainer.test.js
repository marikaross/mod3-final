import { PokemonContainer, mapStateToProps, mapDispatchToProps } from './PokemonContainer.js';

describe('PokemonContainer', () => {
  describe('mSTP', () => {
    it('should return an object with a pokeTypes array', () => {
      const mockState = {pokeTypes: [{name: pokemon}, {name: morePokemon}]}
      const expected = mockSt
    })
  })
})